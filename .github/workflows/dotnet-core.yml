on:
  push:

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - run: .\configure.ps1

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.100-preview.2.20176.6

    - run: .\build.ps1 -SkipUnitTest

    - run: ls $(NuGetClientRoot)\artifacts\VS15
